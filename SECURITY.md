# Security Policy

This repository is public. It must not contain real patient/family/client data, judicial documents, private exports, API secrets, OAuth secrets, access tokens, service-account credentials, private keys or privileged connection strings.

Use fictitious or fully de-identified data in demonstrations. Keep privileged integrations server-side and environment-specific configuration outside version control.

Do not publish sensitive vulnerability details in public issues.
